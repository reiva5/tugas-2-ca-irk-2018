#include "Compressed.h"

unsigned char* toBytes(Compress* compress){
	return "";
}

Compress doCompress(char* namaFile){
	Compress compress;
	return compress;
}

void decompress(char* namaFile){
	return;
}
